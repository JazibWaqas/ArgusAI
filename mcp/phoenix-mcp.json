{
  "mcpServers": {
    "phoenix": {
      "command": "npx",
      "args": [
        "-y",
        "@arizeai/phoenix-mcp@latest",
        "--baseUrl",
        "${PHOENIX_DASHBOARD_URL}",
        "--apiKey",
        "${PHOENIX_API_KEY}"
      ]
    }
  }
}
